import { ConvertToPercentPipe } from './convert-to-percent.pipe';

describe('ConvertToPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
