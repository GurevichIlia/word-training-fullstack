import { VerbConjugationPipe } from './verb-conjugation.pipe';

describe('VerbConjugationPipe', () => {
  it('create an instance', () => {
    const pipe = new VerbConjugationPipe();
    expect(pipe).toBeTruthy();
  });
});
