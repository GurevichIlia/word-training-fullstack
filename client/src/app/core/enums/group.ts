export enum GroupAction {
  NEW_GROUP = 'New group',
  EDIT_GROUP = 'Edit group',
  ADD_WORDS = 'Add words',
  DELETE_GROUP = 'Delete group'
}

export enum GroupType {
  ALL_WORDS = '1',
  FAVORITES = '2'
}
